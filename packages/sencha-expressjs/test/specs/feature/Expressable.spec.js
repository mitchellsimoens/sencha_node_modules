/*const { expect }                            = require('chai');
const { Server, feature : { Expressable } } = require('../../../');*/

describe('feature.Expressable', function() {
    xdescribe('mixed into', function() {
    });
});
